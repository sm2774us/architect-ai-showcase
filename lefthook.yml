# Lefthook configuration
# Modern, lightweight alternative to husky for managing Git hooks

pre-commit:
  commands:
    format-check:
      glob: '*'
      run: npm run format:check
    lint:
      glob: '*'
      run: npm run lint
    unit-tests:
      glob: '*'
      run: npm run test:unit
